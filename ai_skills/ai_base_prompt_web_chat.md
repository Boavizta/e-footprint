I want you to help me create a modeling of the digital carbon footprint of a digital system using the e-footprint Python package (pip install efootprint). Here are the resources you can use to understand e-footprint and manipulate the package in an efficient way to help me do my modeling:

- e-footprint reason for being: https://boavizta.github.io/e-footprint/why_efootprint/
- e-footprint is open source, hosted on github, and this script demonstrates the use of all objects: https://github.com/Boavizta/e-footprint/blob/main/docs_sources/doc_utils/docs_case.py
- every e-footprint class defines default_values, pitfalls, interactions and param_descriptions class attributes that help with understanding how to use the class. Optionally, list_values and conditional_list_values give possible values for constrained parameters.
- The idea is that we’ll discuss the case and you’ll construct the modeling with code, using sensible hypothesis and prompting me for important inputs, especially around system scale and model type for AI modeling. Before writing any code, give me a high-level overview of the system you have in mind, so that we can iterate on it efficiently. When we’re done, you’ll write the code and use the api_utils.system_to_json.system_to_json function to generate a json file that I can upload in the interface.
- If we want to make comparisons between 2 systems (for example, the impact of adding AI to a usage journey or understanding the impact of an ecodesign action), that’s possible too. The e-footprint interface (https://e-footprint.boavizta.org/) allows for the uploading of 2 models and has a comparison view. In that case, you can simply add changes to the reference system after having outputed the first json, and output a second json of the system with changes, which will be the comparison system (they’ll have same ids on their objects, and that’s the point for the comparison. The interface handles that fine).
- Important: when you create timeseries, only use timeseries classes from builders.timeseries that have a form_inputs parameter (ExplainableHourlyQuantitiesFromFormInputs and ExplainableRecurrentQuantitiesFromConstant for now). That’s important because only such timeseries are editable on the interface. Tell the user about the limitations of the dynamics that can be modeled with such timeseries. If the user wants more complex, custom timeseries, they have to accept the tradeoff that the timeseries will only be visualizable in the interface, not editable.
- At the end of the modeling, reflect and give an overview of the system and its particularities to the user. In particular, comment on the scale of the system emissions (like a system emitting only a couple tons of CO2eq per year is probably not worth investing in ecodesigning so much because carbon savings returns on ecodesign investment will be small, but a system emitting in the hundreds or thousands of tons of CO2eq per year should probably be analyzed in more details. Look up https://boavizta.github.io/e-footprint/methodology/ for more details). Also, servers often tend to be over-provisioned. Check the servers’ raw_nb_of_instances against their nb_of_instances to surface possible cases where a lower tier server would be more appropriate (and if you yourself chose the server type, maybe adapt proactively). 

The resources above have been authored by e-footprint’s main maintainer. I myself am new to e-footprint so I’ll be happy to have guidance on the modeling process and how to use the tool.